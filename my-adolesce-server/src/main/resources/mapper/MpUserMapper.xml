<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.adolesce.common.mapper.MpUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.adolesce.common.bo.MpUser">
        <id column="id" property="id" />
        <result column="user_name" property="userName" />
        <result column="password" property="password" />
        <result column="name" property="name" />
        <result column="age" property="age" />
        <result column="phone" property="phone" />
        <result column="birthday" property="birthday" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="card_no" property="cardNo" />
    </resultMap>

    <select id="queryByNameCustom" resultMap="BaseResultMap">
        SELECT `user`.*,record.card_no FROM my_mp_user `user`
        INNER JOIN my_member_record record ON `user`.`name` = record.`client_name`
        WHERE `user`.name LIKE CONCAT('%',#{name},'%')
        ORDER BY `user`.create_time
    </select>


</mapper>
